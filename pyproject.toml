[project]
name = "bridge-finance-network"
version = "0.1.0"
description = "Bridge Finance Network FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "supabase",
    "python-dotenv",
]

[project.scripts]
app = "backend.app.main:app"
